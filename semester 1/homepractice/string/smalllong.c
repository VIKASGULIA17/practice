#include<stdio.h>
#include<string.h>
int main(){
    char str[1024];
    printf("enter vlaue odi stirng");
    gets(str);
    char max=str[0];
    for (int i = 1; i < strlen(str); i++)
    {
        if (str[i]!=" ")
        {
            strcat()
        }
        
        
    }
    printf("%s ",max);
}